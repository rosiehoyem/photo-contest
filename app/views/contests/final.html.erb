<div class="row">
  <div class="col-md-3"></div>
    <div class="col-md-6">
      <h1>The Photo Grand Champion!>
        <h2><%= @image.title %><hw>

        <div class="well">
          <%= image_tag image[1].url, width: "350" %>
        </div>
      </div>
    </div>
  <div class="col-md-3"></div>
</div>

<%= button_to finalize_contests_path(@contest), class: "btn btn-primary" do %>
  <strong>Finalize Tournament</strong>
<% end %>
