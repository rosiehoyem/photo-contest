<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @contest.name %>
</p>

<p>
  <strong>Active round:</strong>
  <%= @contest.active_round %>
  <strong><%=link_to "Vote on this round!", contest_round_path(contest_id: @contest.id, id: @contest.active_round) %></strong>
</p>

<p>
  <strong>Posted:</strong>
  <%= @contest.posted %>
</p>

<%= link_to 'Edit', edit_contest_path(@contest) %> |
<%= link_to 'Back', contests_path %>
